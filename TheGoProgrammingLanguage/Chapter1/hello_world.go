package main // Package main defines a standalone executable program, not a library.

import "fmt"

func main() { // Like many, main is where the execution starts.
	// Semicolon is not required if the there is just one statement in a line.
	fmt.Println("Hello, World!")
}
